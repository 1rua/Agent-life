# Run B — 2026-08-20：方案②修复 + 真实 Tailnet（Path A）尝试与诊断

Run ID 沿用：`2026-08-19T15-10-02Z-p0t-real-device-all`（本文件为本轮补充记录）

## 一、Keystore 方案②修复（用户已确认）

- 修改 `apps/android/tailnet-core/src/main/kotlin/com/openandroidintelligence/tailnet/core/KeystoreEncryptedNoBackupState.kt`：
  加密阶段不再传调用方 IV（Keystore 在 `RandomizedEncryptionRequired(true)` 下手动生成），
  从 `cipher.iv` 读回后写信封；信封格式与 decrypt 不变。
- 设备验证：`:tailnet-core:connectedDebugAndroidTest` 
  `keystoreEncryptedNodeStateSurvivesFreshInstance` **PASS**（12/12 通过，1 条 Assume
  跳过=新加的离线用例未提供束时不算 PASS）。备份见
  `/tmp/KeystoreEncryptedNoBackupState.kt.bak`，修复提案见 `proposals/keystore-fix/`。

## 二、真实 Tailnet（Path A）尝试

- 用户提供一次性 auth key（仅经环境变量入 Go 编码器，**从未落盘/打印/入库**）。
- 用固定 Go `cmd/enrollment-bundle` 生成真实束
  `controlUrl=https://controlplane.tailscale.com`、`pinnedIpv4=100.96.0.1`（不存在的
  Bridge peer）、确定性摘要，推送设备；束仅存 `.toolchains/tsnet-p0t/offline.bundle`
  （git 忽略），证据只记录 sha256 与红act字段。
- 新增 `P0tOfflineRealBackendInstrumentedTest`（Assume-gated）：真实 enroll 后对
  不存在的 pinned peer 调 native `Path()`，期望 backend 真报 OFFLINE 且无 VPN。

## 三、诊断链（诚实记录；OFFLINE 仍未通过）

1. **排障 #1：测试宿主缺 INTERNET 权限**。`com.openandroidintelligence.tailnet.core.test` 无
   INTERNET → Go socket EPERM → ~50ms CONTROL_UNREACHABLE。已在三个 androidTest
   清单加 `INTERNET`（只影响测试 APK，不改正式 app 权限面）。**注意**：此前
   `unreachableControlFailsClosed...` 用例的通过，其触发点实为“权限拒绝”而非
   “网络级控制面拒绝”的语义需要修正——修权限后真机语义应重新核验。
2. **排障 #2：网络层面的可达性**。同一设备 `curl` 严格 TLS 到 controlplane 重试全
   302；`baidu` 200；主机（同出口）Go `http.Get` 到 controlplane 200。→ 网络与证书
   /时钟非阻塞。
3. **排障 #3：AAR 的 Go 运行时解析**。AAR 构建参数 `CGO_ENABLED=1`、无 netgo；
   但设备 **`/etc/resolv.conf` 不存在**且设备为 production（无 root/su，不能补）。
   怀疑 AAR 运行时解析/拨号在该设备上失败 → 仍需 native 探针证实。
4. **进行中**：已交叉编译设备探针 `pure-Go 解析器`（/tmp/goprobe-arm64）与
   `cgo(bionic) 解析器`（/tmp/goprobe-cgo-arm64）。**设备中途掉落 adb**，探针尚未跑完；
   恢复连接后执行 `tools/p0t-device/run-device-go-probes.sh` 即可。

## 四、现状与诚实口径

- 矩阵行：**无新增 PASS**。OFFLINE 仍 BLOCKED（缺“该设备 native 成功 enroll 后
  backend Path()==OFFLINE”的真机证据）。
- 一次性 key 状态：多次尝试均未完成注册（CONTROL_UNREACHABLE / INVALID_BUNDLE），
  大概率未被消耗；但因为无法读取用户 tailnet 控制台，**请用户在
  login.tailscale.com 管理台确认是否出现名为 `p0t-offline-device` 的节点**；若出现
  说明 key 已被用掉/节点已注册（可删除该节点释放）。
- 设备连接：USB 呈 MTP、adb 接口间歇掉线（该项目历史已知问题），需用户重插/重开
  USB 调试后继续探针与 enroll。
