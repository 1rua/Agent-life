---
status: accepted
date: 2026-08-21
---

# 使用有限宿主能力包络与 companion 回退

每个 Android 宿主版本预先声明一组有限的权限、系统组件和内核安全原语，运行时插件无需更新宿主即可自由组合该能力包络。需要当前包络没有的新 Android 权限、前台服务类型、系统注册组件或独立隔离边界时，插件使用 companion APK；动态原生代码和 WASM 都不能凭空扩展已安装宿主的 Manifest。宿主仅在相关插件启用后请求运行时权限，但其发行包仍需如实公开预声明的敏感能力。
