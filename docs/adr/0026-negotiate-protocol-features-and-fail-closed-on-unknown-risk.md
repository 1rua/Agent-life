---
status: accepted
date: 2026-08-24
---

# 协商协议功能并拒绝未知风险语义

Gateway Protocol v2 在连接开始时交换协议主次版本、认证模式、消息与附件版本、事件游标、宿主兼容范围、能力 Schema、附件和队列限制及已验证功能。主版本不一致或出现未知安全字段与高风险操作时拒绝连接；次版本只启用双方共同支持的功能，普通可选字段按 Schema 规则处理，协商结果绑定当前会话且请求不能自行扩权。
