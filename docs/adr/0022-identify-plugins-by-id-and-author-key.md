---
status: accepted
date: 2026-08-24
---

# 以插件 ID 与作者密钥共同标识插件

插件身份由命名空间插件 ID 与作者公钥共同组成，不依赖中央全局 ID 注册；不同作者的同名插件可以共存，但不得共享数据、授权或更新关系。版本采用单调递增的语义化版本，同一身份与版本只能对应一个内容摘要，内容变化而版本不变视为篡改；降级必须由用户明确发起，并在执行前验证数据兼容性。
