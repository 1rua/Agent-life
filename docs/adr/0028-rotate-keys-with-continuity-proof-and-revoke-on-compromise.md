---
status: accepted
date: 2026-08-24
---

# 以连续性证明轮换密钥并在泄露时撤销

Gateway 主密钥在本地事务完成数据重新加密后撤销旧密钥；设备配对密钥以短期双签确认轮换；插件作者密钥、companion 证书和 TLS 公钥使用旧信任材料签署连续性证明并在敏感变化时重新请求用户确认。任何疑似泄露都不使用平滑轮换，而是立即撤销并重新配对或重新建立插件信任；轮换失败保持旧身份可用或明确停用，不进入身份不确定状态。
