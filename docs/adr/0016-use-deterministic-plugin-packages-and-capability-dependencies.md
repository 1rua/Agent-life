---
status: accepted
date: 2026-08-21
---

# 使用确定性插件包与能力依赖

受保护插件与开发者信任插件使用同一种确定性签名插件包，清单在执行任何载荷前声明身份、作者、版本、宿主兼容范围、运行类型、能力、网络、资源、界面、迁移和文件摘要。正式安装对象是固定版本与摘要的产物，而不是可变 Git 工作目录。插件不得直接加载其他插件的代码，只能依赖版本化能力；必需能力缺失时保持停用，可选能力缺失时明确降级，多个提供者由用户选择，依赖关系不传递授权。
