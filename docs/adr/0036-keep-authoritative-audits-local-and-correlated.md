---
status: accepted
date: 2026-08-24
---

# 分别保存本机与 Gateway 权威审计并用关联 ID 对应

Android 对本机插件执行、权限裁决、用户确认和 Companion 调用承担审计权威；每个逻辑 Gateway 对账号认证、设备请求、协议投递和服务端管理承担审计权威。两侧使用共同关联 ID 还原一次跨端操作，但不互相覆盖事实，也不建立中央遥测服务；记录遵守最小化与三十天默认保留规则，正文不进入审计，用户只能通过明确发起的导出合并查看。
