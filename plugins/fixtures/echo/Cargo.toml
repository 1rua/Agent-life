[package]
name = "open-android-intelligence-echo"
version = "0.1.0"
edition = "2021"
description = "Open Android Intelligence echo 参考插件：原样回显请求字节"
publish = false

[lib]
# 产物名固定为 echo.wasm，方便宿主测试按名字取用。
name = "echo"
crate-type = ["cdylib", "rlib"]

[dependencies]
open-android-intelligence-sdk = { path = "../../sdk-rust" }
